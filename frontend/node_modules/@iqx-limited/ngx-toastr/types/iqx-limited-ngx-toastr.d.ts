import * as i0 from '@angular/core';
import { ViewContainerRef, Injector, ComponentRef, InjectionToken, OnDestroy, WritableSignal, EnvironmentProviders, ApplicationRef } from '@angular/core';
import { Observable } from 'rxjs';

declare class ToastContainerDirective {
    private readonly el;
    getContainerElement(): HTMLElement;
    static ɵfac: i0.ɵɵFactoryDeclaration<ToastContainerDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ToastContainerDirective, never, ["toastContainer"], {}, {}, never, never, true, never>;
}

interface ComponentType<T> {
    new (...args: any[]): T;
}
/**
 * A `ComponentPortal` is a portal that instantiates some Component upon attachment.
 */
declare class ComponentPortal<T> {
    /** The type of the component that will be instantiated for attachment. */
    component: ComponentType<T>;
    /**
     * [Optional] Where the attached component should live in Angular's *logical* component tree.
     * This is different from where the component *renders*, which is determined by the PortalHost.
     * The origin necessary when the host is outside of the Angular application context.
     */
    viewContainerRef: ViewContainerRef;
    /** Injector used for the instantiation of the component. */
    injector: Injector;
    private attachedHost?;
    constructor(component: ComponentType<T>, injector: Injector);
    /** Whether this portal is attached to a host. */
    get isAttached(): boolean;
    /** Attach this portal to a host. */
    attach(host: BasePortalHost, newestOnTop: boolean): ComponentRef<any>;
    /** Detach this portal from its host */
    detach(): void;
    /**
     * Sets the PortalHost reference without performing `attach ( )`. This is used directly by
     * the PortalHost when it is performing an `attach ( )` or `detach ( )`.
     */
    setAttachedHost(host?: BasePortalHost): void;
}
/**
 * Partial implementation of PortalHost that only deals with attaching a
 * ComponentPortal
 */
declare abstract class BasePortalHost {
    /** The portal currently attached to the host. */
    private attachedPortal?;
    /** A function that will permanently dispose this host. */
    private disposeFn?;
    attach(portal: ComponentPortal<any>, newestOnTop: boolean): ComponentRef<any>;
    detach(): void;
    setDisposeFn(fn: () => void): void;
    abstract attachComponentPortal<T>(portal: ComponentPortal<T>, newestOnTop: boolean): ComponentRef<T>;
}

/**
 * Reference to an overlay that has been created with the Overlay service.
 * Used to manipulate or dispose of said overlay.
 */
declare class OverlayRef {
    private _portalHost;
    constructor(_portalHost: BasePortalHost);
    attach(portal: ComponentPortal<any>, newestOnTop?: boolean): ComponentRef<any>;
    /**
     * Detaches an overlay from a portal.
     * @returns Resolves when the overlay has been detached.
     */
    detach(): void;
}

/**
 * Reference to a toast opened via the Toastr service.
 */
declare class ToastRef<T> {
    private overlayRef;
    /** The instance of component opened into the toast. */
    componentInstance: T;
    /** Count of duplicates of this toast */
    private duplicatesCount;
    /** Subject for notifying the user that the toast has finished closing. */
    private afterClosed;
    /** triggered when toast is activated */
    private activate;
    /** notifies the toast that it should close before the timeout */
    private manualClose;
    /** notifies the toast that it should reset the timeouts */
    private resetTimeout;
    /** notifies the toast that it should count a duplicate toast */
    private countDuplicate;
    constructor(overlayRef: OverlayRef);
    manualCloseComplete(): void;
    manualClosed(): Observable<any>;
    timeoutReset(): Observable<any>;
    countDuplicateObservable(): Observable<number>;
    /**
     * Close the toast.
     */
    close(): void;
    /** Gets an observable that is notified when the toast is finished closing. */
    afterClosedObservable(): Observable<any>;
    isInactive(): boolean;
    activateComplete(): void;
    /** Gets an observable that is notified when the toast has started opening. */
    afterActivate(): Observable<any>;
    /** Reset the toast timouts and count duplicates */
    onDuplicate(resetTimeout: boolean, countDuplicate: boolean): void;
}

type ProgressAnimationType = "increasing" | "decreasing";
type DisableTimoutType = boolean | "timeOut" | "extendedTimeOut";
/**
 * Configuration for an individual toast.
 */
interface IndividualConfig<ConfigPayload = any> {
    /**
     * disable both timeOut and extendedTimeOut
     * default: false
     */
    disableTimeOut: DisableTimoutType;
    /**
     * toast time to live in milliseconds
     * default: 5000
     */
    timeOut: number;
    /**
     * toast show close button
     * default: false
     */
    closeButton: boolean;
    /**
     * time to close after a user hovers over toast
     * default: 1000
     */
    extendedTimeOut: number;
    /**
     * show toast progress bar
     * default: false
     */
    progressBar: boolean;
    /**
     * changes toast progress bar animation
     * default: decreasing
     */
    progressAnimation: ProgressAnimationType;
    /**
     * render html in toast message (possibly unsafe)
     * default: false
     */
    enableHtml: boolean;
    /**
     * css class on toast component
     * default: ngx-toastr
     */
    toastClass: string;
    /**
     * css class on toast container
     * default: toast-top-right
     */
    positionClass: string;
    /**
     * css class on toast title
     * default: toast-title
     */
    titleClass: string;
    /**
     * css class on toast message
     * default: toast-message
     */
    messageClass: string;
    /**
     * animation easing on toast
     * default: ease-in
     */
    easing: string;
    /**
     * animation ease time on toast
     * default: 300
     */
    easeTime: string | number;
    /**
     * clicking on toast dismisses it
     * default: true
     */
    tapToDismiss: boolean;
    /**
     * Angular toast component to be shown
     * default: Toast
     */
    toastComponent?: ComponentType<any>;
    /**
     * Helps show toast from a websocket or from event outside Angular
     * default: false
     */
    onActivateTick: boolean;
    /**
     * New toast placement
     * default: true
     */
    newestOnTop: boolean;
    /**
     * Payload to pass to the toast component
     */
    payload?: ConfigPayload;
}
interface ToastrIconClasses {
    [key: string]: string;
    error: string;
    info: string;
    success: string;
    warning: string;
}
/**
 * Global Toast configuration
 * Includes all IndividualConfig
 */
interface GlobalConfig extends IndividualConfig {
    /**
     * max toasts opened. Toasts will be queued
     * Zero is unlimited
     * default: 0
     */
    maxOpened: number;
    /**
     * dismiss current toast when max is reached
     * default: false
     */
    autoDismiss: boolean;
    iconClasses: Partial<ToastrIconClasses>;
    /**
     * block duplicate messages
     * default: false
     */
    preventDuplicates: boolean;
    /**
     * display the number of duplicate messages
     * default: false
     */
    countDuplicates: boolean;
    /**
     * Reset toast timeout when there's a duplicate (preventDuplicates needs to be set to true)
     * default: false
     */
    resetTimeoutOnDuplicate: boolean;
    /**
     * consider the title of a toast when checking if duplicate
     * default: false
     */
    includeTitleDuplicates: boolean;
}
/**
 * Everything a toast needs to launch
 */
declare class ToastPackage<ConfigPayload = any> {
    toastId: number;
    config: IndividualConfig<ConfigPayload>;
    message: string | null | undefined;
    title: string | undefined;
    toastType: string;
    toastRef: ToastRef<any>;
    private onTap;
    private onAction;
    constructor(toastId: number, config: IndividualConfig<ConfigPayload>, message: string | null | undefined, title: string | undefined, toastType: string, toastRef: ToastRef<any>);
    /** Fired on click */
    triggerTap(): void;
    onTapObservable(): Observable<void>;
    /** available for use in custom toast */
    triggerAction(action?: any): void;
    onActionObservable(): Observable<void>;
}
declare const DefaultNoComponentGlobalConfig: GlobalConfig;
interface ToastToken {
    default: GlobalConfig;
    config: Partial<GlobalConfig>;
}
declare const TOAST_CONFIG: InjectionToken<ToastToken>;

interface ActiveToast<C> {
    /** Your Toast ID. Use this to close it individually */
    toastId: number;
    /** the title of your toast. Stored to prevent duplicates */
    title: string;
    /** the message of your toast. Stored to prevent duplicates */
    message: string;
    /** a reference to the component see portal.ts */
    portal: ComponentRef<C>;
    /** a reference to your toast */
    toastRef: ToastRef<C>;
    /** triggered when toast is active */
    onShown: Observable<void>;
    /** triggered when toast is destroyed */
    onHidden: Observable<void>;
    /** triggered on toast click */
    onTap: Observable<void>;
    /** available for your use in custom toast */
    onAction: Observable<any>;
}
declare class ToastrService {
    toastrConfig: GlobalConfig;
    currentlyActive: number;
    toasts: ActiveToast<any>[];
    overlayContainer?: ToastContainerDirective;
    previousToastMessage: string | undefined;
    readonly token: ToastToken;
    private readonly overlay;
    private readonly sanitizer;
    private readonly injector;
    private index;
    constructor();
    /** show toast */
    show<ConfigPayload = any>(message?: string, title?: string, override?: Partial<IndividualConfig<ConfigPayload>>, type?: string): ActiveToast<any> | null;
    /** show successful toast */
    success<ConfigPayload = any>(message?: string, title?: string, override?: Partial<IndividualConfig<ConfigPayload>>): ActiveToast<any> | null;
    /** show error toast */
    error<ConfigPayload = any>(message?: string, title?: string, override?: Partial<IndividualConfig<ConfigPayload>>): ActiveToast<any> | null;
    /** show info toast */
    info<ConfigPayload = any>(message?: string, title?: string, override?: Partial<IndividualConfig<ConfigPayload>>): ActiveToast<any> | null;
    /** show warning toast */
    warning<ConfigPayload = any>(message?: string, title?: string, override?: Partial<IndividualConfig<ConfigPayload>>): ActiveToast<any> | null;
    /**
     * Remove all or a single toast by id
     */
    clear(toastId?: number): void;
    /**
     * Remove and destroy a single toast by id
     */
    remove(toastId: number): boolean;
    /**
     * Determines if toast message is already shown
     */
    findDuplicate(title: string | undefined, message: string | undefined, resetOnDuplicate: boolean, countDuplicates: boolean): ActiveToast<any> | null;
    /** create a clone of global config and apply individual settings */
    private applyConfig;
    /**
     * Find toast object by id
     */
    private findToast;
    /**
     * Determines the need to run inside angular's zone then builds the toast
     */
    private preBuildNotification;
    /**
     * Creates and attaches toast data to component
     * returns the active toast, or in case preventDuplicates is enabled the original/non-duplicate active toast.
     */
    private buildNotification;
    static ɵfac: i0.ɵɵFactoryDeclaration<ToastrService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ToastrService>;
}

declare class ToastComponent<ConfigPayload = any> implements OnDestroy {
    message?: string | null;
    title?: string;
    options: IndividualConfig<ConfigPayload>;
    duplicatesCount: number;
    originalTimeout: number;
    /** width of progress bar has increased- */
    width: WritableSignal<number>;
    state: WritableSignal<{
        value: "inactive" | "active" | "removed";
        params: {
            easeTime: number | string;
            easing: string;
        };
    }>;
    readonly toastPackage: ToastPackage;
    protected readonly toastrService: ToastrService;
    private timeout;
    private intervalId;
    private hideTime;
    private sub;
    private sub1;
    private sub2;
    private sub3;
    constructor();
    ngOnDestroy(): void;
    /**
     * activates toast and sets timeout
     */
    activateToast(): void;
    /**
     * updates progress bar width
     */
    updateProgress(): void;
    resetTimeout(): void;
    /**
     * tells toastrService to remove this toast after animation time
     */
    remove(): void;
    /**
     * Handles click/tap on the toast.
     */
    tapToast(): void;
    /**
     * Handles mouseenter to stick the toast around.
     */
    stickAround(): void;
    /**
     * Handles mouseleave to delay hiding the toast.
     */
    delayedHideToast(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ToastComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ToastComponent<any>, "app-toast-component", never, {}, {}, never, never, true, never>;
}

declare const DefaultGlobalConfig: GlobalConfig;
/**
 * @description
 * Provides the `TOAST_CONFIG` token with the given config.
 *
 * @param config The config to configure toastr.
 * @returns The environment providers.
 *
 * @example
 * ```ts
 * import { provideToastr } from 'ngx-toastr';
 *
 * bootstrap(AppComponent, {
 *   providers: [
 *     provideToastr({
 *       timeOut: 2000,
 *       positionClass: 'toast-top-right',
 *     }),
 *   ],
 * })
 */
declare const provideToastr: (config?: Partial<GlobalConfig>) => EnvironmentProviders;
/**
*  @description
*  Provides the `TOAST_CONFIG` token without animations.
*
* @param config The config to configure toastr.
* @returns The environment providers.
*
* @example
* ```ts
* import { provideToastrNoAnimation } from 'ngx-toastr';
*
* bootstrap(AppComponent, {
*   providers: [
*     provideToastrNoAnimation({
*       timeOut: 2000,
*       positionClass: 'toast-top-right',
*     }),
*   ],
* })
* ```
*/
declare const provideToastrNoAnimation: (config?: Partial<GlobalConfig>) => EnvironmentProviders;

declare class ToastNoAnimationComponent implements OnDestroy {
    message?: string | null;
    title?: string;
    options: IndividualConfig;
    duplicatesCount: number;
    originalTimeout: number;
    /** width of progress bar */
    width: i0.WritableSignal<number>;
    /** controls animation */
    state: i0.WritableSignal<string>;
    readonly toastPackage: ToastPackage;
    protected readonly appRef: ApplicationRef;
    protected readonly toastrService: ToastrService;
    private timeout;
    private intervalId;
    private hideTime;
    private sub;
    private sub1;
    private sub2;
    private sub3;
    constructor();
    ngOnDestroy(): void;
    /**
     * Handles click/tap on the toast.
     */
    tapToast(): void;
    /**
     * activates toast and sets timeout
     */
    activateToast(): void;
    /**
     * updates progress bar width
     */
    updateProgress(): void;
    resetTimeout(): void;
    /**
     * tells toastrService to remove this toast after animation time
     */
    remove(): void;
    /**
     * Handles mouseenter to stick the toast around.
     */
    stickAround(): void;
    /**
     * Handles mouseleave to delay hiding the toast.
     */
    delayedHideToast(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ToastNoAnimationComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ToastNoAnimationComponent, "app-toast-noanimation", never, {}, {}, never, never, true, never>;
}
declare const DefaultNoAnimationsGlobalConfig: GlobalConfig;

/**
 * Service to create Overlays. Overlays are dynamically added pieces of floating UI, meant to be
 * used as a low-level building building block for other components. Dialogs, tooltips, menus,
 * selects, etc. can all be built using overlays. The service should primarily be used by authors
 * of re-usable components rather than developers building end-user applications.
 *
 * An overlay *is* a PortalHost, so any kind of Portal can be loaded into one.
 */
declare class Overlay {
    private overlayContainer;
    private appRef;
    private document;
    private paneElements;
    /**
     * Creates an overlay.
     * @returns A reference to the created overlay.
     */
    create(positionClass?: string, overlayContainer?: ToastContainerDirective): OverlayRef;
    getPaneElement(positionClass?: string, overlayContainer?: ToastContainerDirective): HTMLElement;
    /**
     * Creates the DOM element for an overlay and appends it to the overlay container.
     * @returns Newly-created pane element
     */
    private createPaneElement;
    /**
     * Create a DomPortalHost into which the overlay content can be loaded.
     * @param pane The DOM element to turn into a portal host.
     * @returns A portal host for the given DOM element.
     */
    private createPortalHost;
    /**
     * Creates an OverlayRef for an overlay in the given DOM element.
     * @param pane DOM element for the overlay
     */
    private createOverlayRef;
    static ɵfac: i0.ɵɵFactoryDeclaration<Overlay, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<Overlay>;
}

/** Container inside which all toasts will render. */
declare class OverlayContainer implements OnDestroy {
    protected document: Document;
    protected containerElement: HTMLElement;
    ngOnDestroy(): void;
    /**
     * This method returns the overlay container element. It will lazily
     * create the element the first time  it is called to facilitate using
     * the container in non-browser environments.
     * @returns the container element
     */
    getContainerElement(): HTMLElement;
    /**
     * Create the overlay container element, which is simply a div
     * with the 'cdk-overlay-container' class on the document body
     * and 'aria-live="polite"'
     */
    protected createContainer(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<OverlayContainer, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<OverlayContainer>;
}

export { BasePortalHost, ComponentPortal, DefaultGlobalConfig, DefaultNoAnimationsGlobalConfig, DefaultNoComponentGlobalConfig, Overlay, OverlayContainer, OverlayRef, TOAST_CONFIG, ToastComponent, ToastContainerDirective, ToastNoAnimationComponent, ToastPackage, ToastRef, ToastrService, provideToastr, provideToastrNoAnimation };
export type { ActiveToast, ComponentType, DisableTimoutType, GlobalConfig, IndividualConfig, ProgressAnimationType, ToastToken, ToastrIconClasses };
